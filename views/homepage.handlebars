{{#each posts as |post| }}
<div>
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}</a>
        </h2>
        <p>Posted by {{post.user.username}} on </p>
    </div>
    <div>
        <p>
            {{post.content}}
        </p>
    </div>
</div>
{{/each}}